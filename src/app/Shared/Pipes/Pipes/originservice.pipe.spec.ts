import { OriginservicePipe } from './originservice.pipe';

describe('OriginservicePipe', () => {
  it('create an instance', () => {
    const pipe = new OriginservicePipe();
    expect(pipe).toBeTruthy();
  });
});
