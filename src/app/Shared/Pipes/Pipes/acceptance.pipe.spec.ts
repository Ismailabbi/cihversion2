import { AcceptancePipe } from './acceptance.pipe';

describe('AcceptancePipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptancePipe();
    expect(pipe).toBeTruthy();
  });
});
