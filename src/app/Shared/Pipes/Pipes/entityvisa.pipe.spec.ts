import { EntityvisaPipe } from './entityvisa.pipe';

describe('EntityvisaPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityvisaPipe();
    expect(pipe).toBeTruthy();
  });
});
