import { OrginepipePipe } from './orginepipe.pipe';

describe('OrginepipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrginepipePipe();
    expect(pipe).toBeTruthy();
  });
});
