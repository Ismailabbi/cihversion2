import { AcceptanceservicePipe } from './acceptanceservice.pipe';

describe('AcceptanceservicePipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptanceservicePipe();
    expect(pipe).toBeTruthy();
  });
});
