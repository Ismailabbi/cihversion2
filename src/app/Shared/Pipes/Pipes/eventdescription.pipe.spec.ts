import { EventdescriptionPipe } from './eventdescription.pipe';

describe('EventdescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new EventdescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
