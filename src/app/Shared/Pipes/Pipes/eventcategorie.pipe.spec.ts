import { EventcategoriePipe } from './eventcategorie.pipe';

describe('EventcategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new EventcategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
