import { EventfamillePipe } from './eventfamille.pipe';

describe('EventfamillePipe', () => {
  it('create an instance', () => {
    const pipe = new EventfamillePipe();
    expect(pipe).toBeTruthy();
  });
});
