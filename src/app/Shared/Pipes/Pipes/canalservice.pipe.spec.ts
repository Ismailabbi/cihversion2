import { CanalservicePipe } from './canalservice.pipe';

describe('CanalservicePipe', () => {
  it('create an instance', () => {
    const pipe = new CanalservicePipe();
    expect(pipe).toBeTruthy();
  });
});
