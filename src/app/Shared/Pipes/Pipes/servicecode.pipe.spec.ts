import { ServicecodePipe } from './servicecode.pipe';

describe('ServicecodePipe', () => {
  it('create an instance', () => {
    const pipe = new ServicecodePipe();
    expect(pipe).toBeTruthy();
  });
});
