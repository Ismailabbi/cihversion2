import { TypeservicePipe } from './typeservice.pipe';

describe('TypeservicePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeservicePipe();
    expect(pipe).toBeTruthy();
  });
});
