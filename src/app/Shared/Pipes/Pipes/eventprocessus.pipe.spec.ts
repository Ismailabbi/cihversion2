import { EventprocessusPipe } from './eventprocessus.pipe';

describe('EventprocessusPipe', () => {
  it('create an instance', () => {
    const pipe = new EventprocessusPipe();
    expect(pipe).toBeTruthy();
  });
});
